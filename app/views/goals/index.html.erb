<% @goals.each do |goal| %>
    <%= link_to "#{goal.goal_title}", goal_path(goal.id) %>
    <%= goal.description %>
    <%= goal.public ? "Public" : "Private" %>
    <%= goal.completed ? "Completed" : "Incomplete" %>
    <%= button_to "Delete goal", goal_path(:id => goal.id), :method => :delete %>
<% end %>

